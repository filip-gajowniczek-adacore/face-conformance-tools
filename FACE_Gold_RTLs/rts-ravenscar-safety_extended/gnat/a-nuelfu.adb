package body Ada.Numerics.Elementary_Functions is

   ----------
   -- Sqrt --
   ----------

   function Sqrt (X : Float) return Float is
   begin
      return raise Program_Error with "Unimplemented function Sqrt";
   end Sqrt;

   ---------
   -- Log --
   ---------

   function Log (X : Float) return Float is
   begin
      return raise Program_Error with "Unimplemented function Log";
   end Log;

   ---------
   -- Log --
   ---------

   function Log (X, Base : Float) return Float is
   begin
      return raise Program_Error with "Unimplemented function Log";
   end Log;

   ---------
   -- Exp --
   ---------

   function Exp (X : Float) return Float is
   begin
      return raise Program_Error with "Unimplemented function Exp";
   end Exp;

   ----------
   -- "**" --
   ----------

   function "**" (Left, Right : Float) return Float is
   begin
      return raise Program_Error with "Unimplemented function ""**""";
   end "**";

   ---------
   -- Sin --
   ---------

   function Sin (X : Float) return Float is
   begin
      return raise Program_Error with "Unimplemented function Sin";
   end Sin;

   ---------
   -- Sin --
   ---------

   function Sin (X, Cycle : Float) return Float is
   begin
      return raise Program_Error with "Unimplemented function Sin";
   end Sin;

   ---------
   -- Cos --
   ---------

   function Cos (X : Float) return Float is
   begin
      return raise Program_Error with "Unimplemented function Cos";
   end Cos;

   ---------
   -- Cos --
   ---------

   function Cos (X, Cycle : Float) return Float is
   begin
      return raise Program_Error with "Unimplemented function Cos";
   end Cos;

   ---------
   -- Tan --
   ---------

   function Tan (X : Float) return Float is
   begin
      return raise Program_Error with "Unimplemented function Tan";
   end Tan;

   ---------
   -- Tan --
   ---------

   function Tan (X, Cycle : Float) return Float is
   begin
      return raise Program_Error with "Unimplemented function Tan";
   end Tan;

   ---------
   -- Cot --
   ---------

   function Cot (X : Float) return Float is
   begin
      return raise Program_Error with "Unimplemented function Cot";
   end Cot;

   ---------
   -- Cot --
   ---------

   function Cot (X, Cycle : Float) return Float is
   begin
      return raise Program_Error with "Unimplemented function Cot";
   end Cot;

   ------------
   -- Arcsin --
   ------------

   function Arcsin (X : Float) return Float is
   begin
      return raise Program_Error with "Unimplemented function Arcsin";
   end Arcsin;

   ------------
   -- Arcsin --
   ------------

   function Arcsin (X, Cycle : Float) return Float is
   begin
      return raise Program_Error with "Unimplemented function Arcsin";
   end Arcsin;

   ------------
   -- Arccos --
   ------------

   function Arccos (X : Float) return Float is
   begin
      return raise Program_Error with "Unimplemented function Arccos";
   end Arccos;

   ------------
   -- Arccos --
   ------------

   function Arccos (X, Cycle : Float) return Float is
   begin
      return raise Program_Error with "Unimplemented function Arccos";
   end Arccos;

   ------------
   -- Arctan --
   ------------

   function Arctan (Y : Float; X : Float := 1.0) return Float is
   begin
      return raise Program_Error with "Unimplemented function Arctan";
   end Arctan;

   ------------
   -- Arctan --
   ------------

   function Arctan (Y : Float; X : Float := 1.0; Cycle : Float) return Float is
   begin
      return raise Program_Error with "Unimplemented function Arctan";
   end Arctan;

   ------------
   -- Arccot --
   ------------

   function Arccot (X : Float; Y : Float := 1.0) return Float is
   begin
      return raise Program_Error with "Unimplemented function Arccot";
   end Arccot;

   ------------
   -- Arccot --
   ------------

   function Arccot (X : Float; Y : Float := 1.0; Cycle : Float) return Float is
   begin
      return raise Program_Error with "Unimplemented function Arccot";
   end Arccot;

   ------------
   -- C_Sqrt --
   ------------

   function C_Sqrt (X : Float) return Float is
   begin
      return raise Program_Error with "Unimplemented function C_Sqrt";
   end C_Sqrt;

   -----------
   -- C_Log --
   -----------

   function C_Log (X : Float) return Float is
   begin
      return raise Program_Error with "Unimplemented function C_Log";
   end C_Log;

   -----------
   -- C_Exp --
   -----------

   function C_Exp (X : Float) return Float is
   begin
      return raise Program_Error with "Unimplemented function C_Exp";
   end C_Exp;

   -----------
   -- C_Pow --
   -----------

   function C_Pow (X, Y : Float) return Float is
   begin
      return raise Program_Error with "Unimplemented function C_Pow";
   end C_Pow;

   -----------
   -- C_Sin --
   -----------

   function C_Sin (X : Float) return Float is
   begin
      return raise Program_Error with "Unimplemented function C_Sin";
   end C_Sin;

   -----------
   -- C_Cos --
   -----------

   function C_Cos (X : Float) return Float is
   begin
      return raise Program_Error with "Unimplemented function C_Cos";
   end C_Cos;

   -----------
   -- C_Tan --
   -----------

   function C_Tan (X : Float) return Float is
   begin
      return raise Program_Error with "Unimplemented function C_Tan";
   end C_Tan;

   ------------
   -- C_Asin --
   ------------

   function C_Asin (X : Float) return Float is
   begin
      return raise Program_Error with "Unimplemented function C_Asin";
   end C_Asin;

   ------------
   -- C_Acos --
   ------------

   function C_Acos (X : Float) return Float is
   begin
      return raise Program_Error with "Unimplemented function C_Acos";
   end C_Acos;

   -------------
   -- C_Atan2 --
   -------------

   function C_Atan2 (Y, X : Float) return Float is
   begin
      return raise Program_Error with "Unimplemented function C_Atan2";
   end C_Atan2;

end Ada.Numerics.Elementary_Functions;

