package body Ada.Numerics.Generic_Elementary_Functions is

   ----------
   -- Sqrt --
   ----------

   function Sqrt (X : Float_Type'Base) return Float_Type'Base is
   begin
      return raise Program_Error with "Unimplemented function Sqrt";
   end Sqrt;

   ---------
   -- Log --
   ---------

   function Log (X : Float_Type'Base) return Float_Type'Base is
   begin
      return raise Program_Error with "Unimplemented function Log";
   end Log;

   ---------
   -- Log --
   ---------

   function Log (X, Base : Float_Type'Base) return Float_Type'Base is
   begin
      return raise Program_Error with "Unimplemented function Log";
   end Log;

   ---------
   -- Exp --
   ---------

   function Exp (X : Float_Type'Base) return Float_Type'Base is
   begin
      return raise Program_Error with "Unimplemented function Exp";
   end Exp;

   ----------
   -- "**" --
   ----------

   function "**" (Left, Right : Float_Type'Base) return Float_Type'Base is
   begin
      return raise Program_Error with "Unimplemented function ""**""";
   end "**";

   ---------
   -- Sin --
   ---------

   function Sin (X : Float_Type'Base) return Float_Type'Base is
   begin
      return raise Program_Error with "Unimplemented function Sin";
   end Sin;

   ---------
   -- Sin --
   ---------

   function Sin (X, Cycle : Float_Type'Base) return Float_Type'Base is
   begin
      return raise Program_Error with "Unimplemented function Sin";
   end Sin;

   ---------
   -- Cos --
   ---------

   function Cos (X : Float_Type'Base) return Float_Type'Base is
   begin
      return raise Program_Error with "Unimplemented function Cos";
   end Cos;

   ---------
   -- Cos --
   ---------

   function Cos (X, Cycle : Float_Type'Base) return Float_Type'Base is
   begin
      return raise Program_Error with "Unimplemented function Cos";
   end Cos;

   ---------
   -- Tan --
   ---------

   function Tan (X : Float_Type'Base) return Float_Type'Base is
   begin
      return raise Program_Error with "Unimplemented function Tan";
   end Tan;

   ---------
   -- Tan --
   ---------

   function Tan (X, Cycle : Float_Type'Base) return Float_Type'Base is
   begin
      return raise Program_Error with "Unimplemented function Tan";
   end Tan;

   ---------
   -- Cot --
   ---------

   function Cot (X : Float_Type'Base) return Float_Type'Base is
   begin
      return raise Program_Error with "Unimplemented function Cot";
   end Cot;

   ---------
   -- Cot --
   ---------

   function Cot (X, Cycle : Float_Type'Base) return Float_Type'Base is
   begin
      return raise Program_Error with "Unimplemented function Cot";
   end Cot;

   ------------
   -- Arcsin --
   ------------

   function Arcsin (X : Float_Type'Base) return Float_Type'Base is
   begin
      return raise Program_Error with "Unimplemented function Arcsin";
   end Arcsin;

   ------------
   -- Arcsin --
   ------------

   function Arcsin (X, Cycle : Float_Type'Base) return Float_Type'Base is
   begin
      return raise Program_Error with "Unimplemented function Arcsin";
   end Arcsin;

   ------------
   -- Arccos --
   ------------

   function Arccos (X : Float_Type'Base) return Float_Type'Base is
   begin
      return raise Program_Error with "Unimplemented function Arccos";
   end Arccos;

   ------------
   -- Arccos --
   ------------

   function Arccos (X, Cycle : Float_Type'Base) return Float_Type'Base is
   begin
      return raise Program_Error with "Unimplemented function Arccos";
   end Arccos;

   ------------
   -- Arctan --
   ------------

   function Arctan
     (Y : Float_Type'Base; X : Float_Type'Base := 1.0) return Float_Type'Base
   is
   begin
      return raise Program_Error with "Unimplemented function Arctan";
   end Arctan;

   ------------
   -- Arctan --
   ------------

   function Arctan
     (Y : Float_Type'Base; X : Float_Type'Base := 1.0; Cycle : Float_Type'Base)
      return Float_Type'Base
   is
   begin
      return raise Program_Error with "Unimplemented function Arctan";
   end Arctan;

   ------------
   -- Arccot --
   ------------

   function Arccot
     (X : Float_Type'Base; Y : Float_Type'Base := 1.0) return Float_Type'Base
   is
   begin
      return raise Program_Error with "Unimplemented function Arccot";
   end Arccot;

   ------------
   -- Arccot --
   ------------

   function Arccot
     (X : Float_Type'Base; Y : Float_Type'Base := 1.0; Cycle : Float_Type'Base)
      return Float_Type'Base
   is
   begin
      return raise Program_Error with "Unimplemented function Arccot";
   end Arccot;

end Ada.Numerics.Generic_Elementary_Functions;

