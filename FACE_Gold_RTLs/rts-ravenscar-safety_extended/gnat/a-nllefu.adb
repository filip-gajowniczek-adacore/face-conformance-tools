package body Ada.Numerics.Long_Long_Elementary_Functions is

   ----------
   -- Sqrt --
   ----------

   function Sqrt (X : Long_Long_Float) return Long_Long_Float is
   begin
      return raise Program_Error with "Unimplemented function Sqrt";
   end Sqrt;

   ---------
   -- Log --
   ---------

   function Log (X : Long_Long_Float) return Long_Long_Float is
   begin
      return raise Program_Error with "Unimplemented function Log";
   end Log;

   ---------
   -- Log --
   ---------

   function Log (X, Base : Long_Long_Float) return Long_Long_Float is
   begin
      return raise Program_Error with "Unimplemented function Log";
   end Log;

   ---------
   -- Exp --
   ---------

   function Exp (X : Long_Long_Float) return Long_Long_Float is
   begin
      return raise Program_Error with "Unimplemented function Exp";
   end Exp;

   ----------
   -- "**" --
   ----------

   function "**" (Left, Right : Long_Long_Float) return Long_Long_Float is
   begin
      return raise Program_Error with "Unimplemented function ""**""";
   end "**";

   ---------
   -- Sin --
   ---------

   function Sin (X : Long_Long_Float) return Long_Long_Float is
   begin
      return raise Program_Error with "Unimplemented function Sin";
   end Sin;

   ---------
   -- Sin --
   ---------

   function Sin (X, Cycle : Long_Long_Float) return Long_Long_Float is
   begin
      return raise Program_Error with "Unimplemented function Sin";
   end Sin;

   ---------
   -- Cos --
   ---------

   function Cos (X : Long_Long_Float) return Long_Long_Float is
   begin
      return raise Program_Error with "Unimplemented function Cos";
   end Cos;

   ---------
   -- Cos --
   ---------

   function Cos (X, Cycle : Long_Long_Float) return Long_Long_Float is
   begin
      return raise Program_Error with "Unimplemented function Cos";
   end Cos;

   ---------
   -- Tan --
   ---------

   function Tan (X : Long_Long_Float) return Long_Long_Float is
   begin
      return raise Program_Error with "Unimplemented function Tan";
   end Tan;

   ---------
   -- Tan --
   ---------

   function Tan (X, Cycle : Long_Long_Float) return Long_Long_Float is
   begin
      return raise Program_Error with "Unimplemented function Tan";
   end Tan;

   ---------
   -- Cot --
   ---------

   function Cot (X : Long_Long_Float) return Long_Long_Float is
   begin
      return raise Program_Error with "Unimplemented function Cot";
   end Cot;

   ---------
   -- Cot --
   ---------

   function Cot (X, Cycle : Long_Long_Float) return Long_Long_Float is
   begin
      return raise Program_Error with "Unimplemented function Cot";
   end Cot;

   ------------
   -- Arcsin --
   ------------

   function Arcsin (X : Long_Long_Float) return Long_Long_Float is
   begin
      return raise Program_Error with "Unimplemented function Arcsin";
   end Arcsin;

   ------------
   -- Arcsin --
   ------------

   function Arcsin (X, Cycle : Long_Long_Float) return Long_Long_Float is
   begin
      return raise Program_Error with "Unimplemented function Arcsin";
   end Arcsin;

   ------------
   -- Arccos --
   ------------

   function Arccos (X : Long_Long_Float) return Long_Long_Float is
   begin
      return raise Program_Error with "Unimplemented function Arccos";
   end Arccos;

   ------------
   -- Arccos --
   ------------

   function Arccos (X, Cycle : Long_Long_Float) return Long_Long_Float is
   begin
      return raise Program_Error with "Unimplemented function Arccos";
   end Arccos;

   ------------
   -- Arctan --
   ------------

   function Arctan
     (Y : Long_Long_Float; X : Long_Long_Float := 1.0) return Long_Long_Float
   is
   begin
      return raise Program_Error with "Unimplemented function Arctan";
   end Arctan;

   ------------
   -- Arctan --
   ------------

   function Arctan
     (Y : Long_Long_Float; X : Long_Long_Float := 1.0; Cycle : Long_Long_Float)
      return Long_Long_Float
   is
   begin
      return raise Program_Error with "Unimplemented function Arctan";
   end Arctan;

   ------------
   -- Arccot --
   ------------

   function Arccot
     (X : Long_Long_Float; Y : Long_Long_Float := 1.0) return Long_Long_Float
   is
   begin
      return raise Program_Error with "Unimplemented function Arccot";
   end Arccot;

   ------------
   -- Arccot --
   ------------

   function Arccot
     (X : Long_Long_Float; Y : Long_Long_Float := 1.0; Cycle : Long_Long_Float)
      return Long_Long_Float
   is
   begin
      return raise Program_Error with "Unimplemented function Arccot";
   end Arccot;

end Ada.Numerics.Long_Long_Elementary_Functions;

