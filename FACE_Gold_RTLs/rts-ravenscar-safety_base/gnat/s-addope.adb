package body System.Address_Operations is

   ----------
   -- AddA --
   ----------

   function AddA (Left, Right : Address) return Address is
   begin
      return raise Program_Error with "Unimplemented function AddA";
   end AddA;

   ----------
   -- SubA --
   ----------

   function SubA (Left, Right : Address) return Address is
   begin
      return raise Program_Error with "Unimplemented function SubA";
   end SubA;

   ----------
   -- MulA --
   ----------

   function MulA (Left, Right : Address) return Address is
   begin
      return raise Program_Error with "Unimplemented function MulA";
   end MulA;

   ----------
   -- DivA --
   ----------

   function DivA (Left, Right : Address) return Address is
   begin
      return raise Program_Error with "Unimplemented function DivA";
   end DivA;

   ----------
   -- ModA --
   ----------

   function ModA (Left, Right : Address) return Address is
   begin
      return raise Program_Error with "Unimplemented function ModA";
   end ModA;

   ----------
   -- AndA --
   ----------

   function AndA (Left, Right : Address) return Address is
   begin
      return raise Program_Error with "Unimplemented function AndA";
   end AndA;

   ---------
   -- OrA --
   ---------

   function OrA (Left, Right : Address) return Address is
   begin
      return raise Program_Error with "Unimplemented function OrA";
   end OrA;

end System.Address_Operations;

